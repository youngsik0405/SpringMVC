<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<!-- ○ mapper 엘리먼트의 『namespace="패키기 경로가 포함된 인터페이스』 -->
<!-- 	- 인터페이스 내부에 메소드가 선언되어 있어야 한다. -->
<!--  	- 선언된 메소드의 이름과 『id="식별자"』 가 일치해야 한다. -->
<mapper namespace="com.test.mybatis.IGradeDAO">

	<select id="list" resultType="com.test.mybatis.GradeDTO">
		SELECT SID, NAME, SUB1, SUB2, SUB3, TOT, AVG, CH
		FROM GRADEVIEW
	</select>

	<insert id="add">
		INSERT INTO TBL_GRADE(SID, SUB1, SUB2, SUB3)
		VALUES(#{sid}, #{sub1}, #{sub2}, #{sub3})
	</insert>
	
	<select id="search" resultType="com.test.mybatis.GradeDTO">
		SELECT *
		FROM GRADEVIEW
		WHERE SID = #{sid}
	</select>

	<update id="update">
		UPDATE TBL_GRADE
		SET SUB1=#{sub1}, SUB2=#{sub2}, SUB3=#{sub3}
		WHERE SID=#{sid}
	</update>
	
	<delete id="delete">
		DELETE
		FROM TBL_GRADE
		WHERE SID=#{sid}
	</delete>

</mapper>
